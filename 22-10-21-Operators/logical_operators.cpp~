#include <iostream>

int main (void)
{
	std::cout << ((5==5) && (3>6)) << std::endl;
	std::cout << ((5==5) || (3>6)) << std::endl;

	int flag = (5<4) ? -1 : 32;
	std::cout << flag << std::endl;

	return 0;
}
